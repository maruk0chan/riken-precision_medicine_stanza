import { S as SvelteComponent, i as init, s as safe_not_equal, C as handle_promise, e as element, a as space, b as attr, c as insert, d as append, D as update_await_block_branch, m as transition_in, p as transition_out, f as detach, u as create_component, t as text, v as mount_component, n as noop, w as destroy_component, y as empty, r as group_outros, o as check_outros, g as destroy_each, q as src_url_equal, k as Stanza, l as defineStanzaElement } from './index-e41a6caa.js';
import { t as toCamelCase } from './CamelCase-d435ec30.js';
import { F as Fa, a as faTriangleExclamation, f as faCircleChevronRight } from './index-97bbddfa.js';
import { d as drugIcon } from './drug-97ae0c78.js';

/* stanzas/search-disease/App.svelte generated by Svelte v3.58.0 */

function get_each_context(ctx, list, i) {
	const child_ctx = ctx.slice();
	child_ctx[5] = list[i].MGeND_DiseaseName;
	child_ctx[6] = list[i].ClinVar_DiseaseName;
	child_ctx[7] = list[i].genename;
	child_ctx[8] = list[i].uniprot_acc;
	child_ctx[9] = list[i].variant;
	child_ctx[10] = list[i].MGeND_ClinicalSignificance;
	child_ctx[11] = list[i].ClinVar_ClinicalSignificance;
	child_ctx[12] = list[i].calculation_type;
	child_ctx[0] = list[i].assembly;
	child_ctx[13] = list[i].chr;
	child_ctx[14] = list[i].alt;
	child_ctx[15] = list[i].ref;
	child_ctx[16] = list[i].start;
	child_ctx[17] = list[i].end;
	child_ctx[18] = list[i].Compound_ID;
	child_ctx[19] = list[i].PDB_ID;
	return child_ctx;
}

function get_each_context_1(ctx, list, i) {
	const child_ctx = ctx.slice();
	child_ctx[22] = list[i];
	return child_ctx;
}

// (104:6) {:catch error}
function create_catch_block(ctx) {
	let tr;
	let td;
	let fa;
	let t;
	let br;
	let current;

	fa = new Fa({
			props: {
				icon: faTriangleExclamation,
				size: "90%",
				color: "var(--warning-color)"
			}
		});

	return {
		c() {
			tr = element("tr");
			td = element("td");
			create_component(fa.$$.fragment);
			t = text("\n            Unable to fetch data from the server. Please refresh the page or try\n            again later.");
			br = element("br");
			attr(td, "class", "error-message");
			attr(td, "colspan", "10");
		},
		m(target, anchor) {
			insert(target, tr, anchor);
			append(tr, td);
			mount_component(fa, td, null);
			append(td, t);
			append(td, br);
			current = true;
		},
		p: noop,
		i(local) {
			if (current) return;
			transition_in(fa.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(fa.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(tr);
			destroy_component(fa);
		}
	};
}

// (42:6) {:then dataset}
function create_then_block(ctx) {
	let each_1_anchor;
	let current;
	let each_value = /*dataset*/ ctx[4].data;
	let each_blocks = [];

	for (let i = 0; i < each_value.length; i += 1) {
		each_blocks[i] = create_each_block(get_each_context(ctx, each_value, i));
	}

	const out = i => transition_out(each_blocks[i], 1, 1, () => {
		each_blocks[i] = null;
	});

	return {
		c() {
			for (let i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].c();
			}

			each_1_anchor = empty();
		},
		m(target, anchor) {
			for (let i = 0; i < each_blocks.length; i += 1) {
				if (each_blocks[i]) {
					each_blocks[i].m(target, anchor);
				}
			}

			insert(target, each_1_anchor, anchor);
			current = true;
		},
		p(ctx, dirty) {
			if (dirty & /*promise, window, faCircleChevronRight, drugIcon*/ 2) {
				each_value = /*dataset*/ ctx[4].data;
				let i;

				for (i = 0; i < each_value.length; i += 1) {
					const child_ctx = get_each_context(ctx, each_value, i);

					if (each_blocks[i]) {
						each_blocks[i].p(child_ctx, dirty);
						transition_in(each_blocks[i], 1);
					} else {
						each_blocks[i] = create_each_block(child_ctx);
						each_blocks[i].c();
						transition_in(each_blocks[i], 1);
						each_blocks[i].m(each_1_anchor.parentNode, each_1_anchor);
					}
				}

				group_outros();

				for (i = each_value.length; i < each_blocks.length; i += 1) {
					out(i);
				}

				check_outros();
			}
		},
		i(local) {
			if (current) return;

			for (let i = 0; i < each_value.length; i += 1) {
				transition_in(each_blocks[i]);
			}

			current = true;
		},
		o(local) {
			each_blocks = each_blocks.filter(Boolean);

			for (let i = 0; i < each_blocks.length; i += 1) {
				transition_out(each_blocks[i]);
			}

			current = false;
		},
		d(detaching) {
			destroy_each(each_blocks, detaching);
			if (detaching) detach(each_1_anchor);
		}
	};
}

// (85:15) {#if calculation_type.length > 0}
function create_if_block(ctx) {
	let each_1_anchor;
	let current;
	let each_value_1 = /*calculation_type*/ ctx[12];
	let each_blocks = [];

	for (let i = 0; i < each_value_1.length; i += 1) {
		each_blocks[i] = create_each_block_1(get_each_context_1(ctx, each_value_1, i));
	}

	const out = i => transition_out(each_blocks[i], 1, 1, () => {
		each_blocks[i] = null;
	});

	return {
		c() {
			for (let i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].c();
			}

			each_1_anchor = empty();
		},
		m(target, anchor) {
			for (let i = 0; i < each_blocks.length; i += 1) {
				if (each_blocks[i]) {
					each_blocks[i].m(target, anchor);
				}
			}

			insert(target, each_1_anchor, anchor);
			current = true;
		},
		p(ctx, dirty) {
			if (dirty & /*window, promise, faCircleChevronRight, drugIcon*/ 2) {
				each_value_1 = /*calculation_type*/ ctx[12];
				let i;

				for (i = 0; i < each_value_1.length; i += 1) {
					const child_ctx = get_each_context_1(ctx, each_value_1, i);

					if (each_blocks[i]) {
						each_blocks[i].p(child_ctx, dirty);
						transition_in(each_blocks[i], 1);
					} else {
						each_blocks[i] = create_each_block_1(child_ctx);
						each_blocks[i].c();
						transition_in(each_blocks[i], 1);
						each_blocks[i].m(each_1_anchor.parentNode, each_1_anchor);
					}
				}

				group_outros();

				for (i = each_value_1.length; i < each_blocks.length; i += 1) {
					out(i);
				}

				check_outros();
			}
		},
		i(local) {
			if (current) return;

			for (let i = 0; i < each_value_1.length; i += 1) {
				transition_in(each_blocks[i]);
			}

			current = true;
		},
		o(local) {
			each_blocks = each_blocks.filter(Boolean);

			for (let i = 0; i < each_blocks.length; i += 1) {
				transition_out(each_blocks[i]);
			}

			current = false;
		},
		d(detaching) {
			destroy_each(each_blocks, detaching);
			if (detaching) detach(each_1_anchor);
		}
	};
}

// (86:16) {#each calculation_type as calc}
function create_each_block_1(ctx) {
	let a;
	let img;
	let img_src_value;
	let t0;
	let t1_value = /*calc*/ ctx[22] + "";
	let t1;
	let t2;
	let fa;
	let t3;
	let current;

	fa = new Fa({
			props: {
				icon: faCircleChevronRight,
				size: "90%",
				color: "var(--calc-color)"
			}
		});

	return {
		c() {
			a = element("a");
			img = element("img");
			t0 = space();
			t1 = text(t1_value);
			t2 = space();
			create_component(fa.$$.fragment);
			t3 = space();
			attr(img, "class", "icon");
			if (!src_url_equal(img.src, img_src_value = drugIcon)) attr(img, "src", img_src_value);
			attr(img, "alt", "drug");
			attr(a, "class", "link-calc");
			attr(a, "href", `${window.location.origin}/dev/calculation/details?assembly=${/*assembly*/ ctx[0]}&genename=${/*genename*/ ctx[7]}&calculation_type=${/*calc*/ ctx[22]}&Compound_ID=${/*Compound_ID*/ ctx[18]}&PDB_ID=${/*PDB_ID*/ ctx[19]}&variant=${/*variant*/ ctx[9]}`);
		},
		m(target, anchor) {
			insert(target, a, anchor);
			append(a, img);
			append(a, t0);
			append(a, t1);
			append(a, t2);
			mount_component(fa, a, null);
			append(a, t3);
			current = true;
		},
		p: noop,
		i(local) {
			if (current) return;
			transition_in(fa.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(fa.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(a);
			destroy_component(fa);
		}
	};
}

// (43:8) {#each dataset.data as { MGeND_DiseaseName, ClinVar_DiseaseName, genename, uniprot_acc, variant, MGeND_ClinicalSignificance, ClinVar_ClinicalSignificance, calculation_type, assembly, chr, alt, ref, start, end, Compound_ID, PDB_ID }}
function create_each_block(ctx) {
	let tr;
	let td0;
	let raw0_value = /*MGeND_DiseaseName*/ ctx[5].join("<br>") + "";
	let t0;
	let td1;
	let raw1_value = /*ClinVar_DiseaseName*/ ctx[6].join("<br>") + "";
	let t1;
	let td2;
	let a0;
	let t2_value = /*genename*/ ctx[7] + "";
	let t2;
	let fa0;
	let t3;
	let td3;
	let t4_value = /*uniprot_acc*/ ctx[8] + "";
	let t4;
	let t5;
	let td4;
	let a1;
	let t6_value = /*variant*/ ctx[9] + "";
	let t6;
	let fa1;
	let t7;
	let td5;

	let t8_value = (/*MGeND_ClinicalSignificance*/ ctx[10].length === 0
	? "-"
	: /*MGeND_ClinicalSignificance*/ ctx[10]) + "";

	let t8;
	let t9;
	let td6;

	let t10_value = (/*ClinVar_ClinicalSignificance*/ ctx[11].length === 0
	? "-"
	: /*ClinVar_ClinicalSignificance*/ ctx[11]) + "";

	let t10;
	let t11;
	let td7;
	let t12;
	let current;

	fa0 = new Fa({
			props: {
				icon: faCircleChevronRight,
				size: "90%",
				color: "var(--gene-color)"
			}
		});

	fa1 = new Fa({
			props: {
				icon: faCircleChevronRight,
				size: "90%",
				color: "var(--variant-color)"
			}
		});

	let if_block = /*calculation_type*/ ctx[12].length > 0 && create_if_block(ctx);

	return {
		c() {
			tr = element("tr");
			td0 = element("td");
			t0 = space();
			td1 = element("td");
			t1 = space();
			td2 = element("td");
			a0 = element("a");
			t2 = text(t2_value);
			create_component(fa0.$$.fragment);
			t3 = space();
			td3 = element("td");
			t4 = text(t4_value);
			t5 = space();
			td4 = element("td");
			a1 = element("a");
			t6 = text(t6_value);
			create_component(fa1.$$.fragment);
			t7 = space();
			td5 = element("td");
			t8 = text(t8_value);
			t9 = space();
			td6 = element("td");
			t10 = text(t10_value);
			t11 = space();
			td7 = element("td");
			if (if_block) if_block.c();
			t12 = space();
			attr(td0, "class", "td-disease");
			attr(td1, "class", "td-disease");
			attr(a0, "class", "link-gene");
			attr(a0, "href", `${window.location.origin}/dev/genes/details?uniprot_acc=${/*uniprot_acc*/ ctx[8]}&assembly=${/*assembly*/ ctx[0]}&genename=${/*genename*/ ctx[7]}`);
			attr(a1, "class", "link-variant");
			attr(a1, "href", `${window.location.origin}/dev/variants/details?assembly=${/*assembly*/ ctx[0]}&chr=${/*chr*/ ctx[13] ? /*chr*/ ctx[13] : "chr2"}&start=${/*start*/ ctx[16]}&end=${/*end*/ ctx[17]}&alt=${/*alt*/ ctx[14]}&ref=${/*ref*/ ctx[15]}&variant=${/*variant*/ ctx[9]}`);
			attr(td7, "class", "td-calc");
		},
		m(target, anchor) {
			insert(target, tr, anchor);
			append(tr, td0);
			td0.innerHTML = raw0_value;
			append(tr, t0);
			append(tr, td1);
			td1.innerHTML = raw1_value;
			append(tr, t1);
			append(tr, td2);
			append(td2, a0);
			append(a0, t2);
			mount_component(fa0, a0, null);
			append(tr, t3);
			append(tr, td3);
			append(td3, t4);
			append(tr, t5);
			append(tr, td4);
			append(td4, a1);
			append(a1, t6);
			mount_component(fa1, a1, null);
			append(tr, t7);
			append(tr, td5);
			append(td5, t8);
			append(tr, t9);
			append(tr, td6);
			append(td6, t10);
			append(tr, t11);
			append(tr, td7);
			if (if_block) if_block.m(td7, null);
			append(tr, t12);
			current = true;
		},
		p(ctx, dirty) {
			if (/*calculation_type*/ ctx[12].length > 0) if_block.p(ctx, dirty);
		},
		i(local) {
			if (current) return;
			transition_in(fa0.$$.fragment, local);
			transition_in(fa1.$$.fragment, local);
			transition_in(if_block);
			current = true;
		},
		o(local) {
			transition_out(fa0.$$.fragment, local);
			transition_out(fa1.$$.fragment, local);
			transition_out(if_block);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(tr);
			destroy_component(fa0);
			destroy_component(fa1);
			if (if_block) if_block.d();
		}
	};
}

// (40:22)          <tr><td colspan="10">Loading...</td></tr>       {:then dataset}
function create_pending_block(ctx) {
	let tr;

	return {
		c() {
			tr = element("tr");
			tr.innerHTML = `<td colspan="10">Loading...</td>`;
		},
		m(target, anchor) {
			insert(target, tr, anchor);
		},
		p: noop,
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(tr);
		}
	};
}

function create_fragment(ctx) {
	let div;
	let table;
	let thead;
	let t15;
	let tbody;
	let current;

	let info = {
		ctx,
		current: null,
		token: null,
		hasCatch: true,
		pending: create_pending_block,
		then: create_then_block,
		catch: create_catch_block,
		value: 4,
		error: 25,
		blocks: [,,,]
	};

	handle_promise(/*promise*/ ctx[1], info);

	return {
		c() {
			div = element("div");
			table = element("table");
			thead = element("thead");

			thead.innerHTML = `<tr><th class="th-disease">Name (MGeND)</th> 
        <th class="th-disease">Name (ClinVar)</th> 
        <th class="th-gene">Gene</th> 
        <th class="th-gene">UniProt acc</th> 
        <th class="th-variant">Variant</th> 
        <th class="th-disease">MGeND Significance</th> 
        <th class="th-disease">ClinVar Significance</th> 
        <th class="th-calc">Calculated</th></tr>`;

			t15 = space();
			tbody = element("tbody");
			info.block.c();
			attr(table, "class", "table");
			attr(div, "class", "search-disease");
		},
		m(target, anchor) {
			insert(target, div, anchor);
			append(div, table);
			append(table, thead);
			append(table, t15);
			append(table, tbody);
			info.block.m(tbody, info.anchor = null);
			info.mount = () => tbody;
			info.anchor = null;
			current = true;
		},
		p(new_ctx, [dirty]) {
			ctx = new_ctx;
			update_await_block_branch(info, ctx, dirty);
		},
		i(local) {
			if (current) return;
			transition_in(info.block);
			current = true;
		},
		o(local) {
			for (let i = 0; i < 3; i += 1) {
				const block = info.blocks[i];
				transition_out(block);
			}

			current = false;
		},
		d(detaching) {
			if (detaching) detach(div);
			info.block.d();
			info.token = null;
			info = null;
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { assembly, term } = $$props;
	let promise = search(term);

	async function search(disease) {
		const response = await fetch(`https://precisionmd-db.med.kyoto-u.ac.jp/api/positions/search?assembly=${assembly}&disease=${disease}`);
		const json = await response.json();

		if (response.ok) {
			return json;
		} else {
			throw new Error(json);
		}
	}

	$$self.$$set = $$props => {
		if ('assembly' in $$props) $$invalidate(0, assembly = $$props.assembly);
		if ('term' in $$props) $$invalidate(2, term = $$props.term);
	};

	return [assembly, promise, term];
}

class App extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { assembly: 0, term: 2 });
	}
}

class SearchDisease extends Stanza {
  app = null;

  async render() {
    if (this.app) {
      this.root.querySelector("main > .search-disease").remove();
    }
    this.app = new App({
      target: this.root.querySelector("main"),
      props: {
        assembly: toCamelCase(this.params)["stanzaAssembly"],
        term: toCamelCase(this.params)["stanzaTerm"],
        root: this.root.querySelector("main"),
      },
    });
  }
}

var stanzaModule = /*#__PURE__*/Object.freeze({
  __proto__: null,
  'default': SearchDisease
});

var metadata = {
	"@context": {
	stanza: "http://togostanza.org/resource/stanza#"
},
	"@id": "search-disease",
	"stanza:label": "Search Disease",
	"stanza:definition": "For PrecisionMD-DB",
	"stanza:license": "MIT",
	"stanza:author": "PENQE",
	"stanza:contributor": [
],
	"stanza:created": "2023-04-21",
	"stanza:updated": "2023-04-21",
	"stanza:parameter": [
	{
		"stanza:key": "stanza-assembly",
		"stanza:example": "hg38",
		"stanza:description": "Assembly"
	},
	{
		"stanza:key": "stanza-Term",
		"stanza:example": "cancer",
		"stanza:description": "Disease"
	}
],
	"stanza:menu-placement": "none",
	"stanza:style": [
	{
		"stanza:key": "--togostanza-canvas-height",
		"stanza:type": "number",
		"stanza:default": 350,
		"stanza:description": "Canvas height"
	},
	{
		"stanza:key": "--togostanza-theme-background_color",
		"stanza:type": "color",
		"stanza:default": "#11435c",
		"stanza:description": "Background color"
	},
	{
		"stanza:key": "--togostanza-fonts-font_family",
		"stanza:type": "text",
		"stanza:default": "Helvetica Neue, Arial, sans-serif",
		"stanza:description": "Font family"
	},
	{
		"stanza:key": "--togostanza-fonts-font_color",
		"stanza:type": "color",
		"stanza:default": "#ffffff",
		"stanza:description": "Text color"
	},
	{
		"stanza:key": "--togostanza-fonts-font_size_primary",
		"stanza:type": "number",
		"stanza:default": 14,
		"stanza:description": "Primary font size"
	},
	{
		"stanza:key": "--togostanza-fonts-font_align",
		"stanza:type": "single-choice",
		"stanza:choice": [
			"left",
			"center",
			"right"
		],
		"stanza:default": "center",
		"stanza:description": "text align of greeting"
	}
],
	"stanza:incomingEvent": [
],
	"stanza:outgoingEvent": [
]
};

var templates = [
  
];

const url = import.meta.url.replace(/\?.*$/, '');

defineStanzaElement({stanzaModule, metadata, templates, url});
//# sourceMappingURL=search-disease.js.map
